// db.js
async function evaluateSQL({ userId, contestId, questionId, sql }) {
  // Simulate execution (would be replaced by actual logic or mocked in tests)
  return {
    isCorrect: false,
    message: "Not implemented",
    expected: [],
    received: [],
  };
}

module.exports = { evaluateSQL };
